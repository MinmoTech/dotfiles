#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

PS1='[\u@\h \W]\$ '
[ -r /home/julius/.byobu/prompt ] && . /home/julius/.byobu/prompt   #byobu-prompt#
# eval "$(pyenv init -)"
